import { TestBed, async, inject } from '@angular/core/testing';

import { SearchedGuard } from './searched.guard';

// describe('SearchedGuard', () => {
//   beforeEach(() => {
//     TestBed.configureTestingModule({
//       providers: [SearchedGuard]
//     });
//   });

//   it('should ...', inject([SearchedGuard], (guard: SearchedGuard) => {
//     expect(guard).toBeTruthy();
//   }));
// });
